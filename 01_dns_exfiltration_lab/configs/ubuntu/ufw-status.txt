Status: active
Logging: on (low)
Default: deny (incoming), deny (outgoing), disabled (routed)
New profiles: skip

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW IN    172.16.99.0/24             # Allow SSH from management VLAN
53                         ALLOW IN    172.16.10.0/24             # Allow DNS from victim VLAN
53                         ALLOW IN    192.168.66.0/24            # Allow DNS from VLAN 66
53                         ALLOW IN    172.16.99.0/24             # Allow DNS from management VLAN

172.16.99.0/24             ALLOW OUT   Anywhere                   # Allow outbound to management VLAN
192.168.66.0/24            ALLOW OUT   Anywhere                   # Allow outbound to VLAN 66

