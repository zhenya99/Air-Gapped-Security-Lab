! DNS lab overlay for the documented Catalyst 2960-X baseline.
! Apply from console or analyst 172.16.99.10 after pre-change backup.
! Preserve existing users, passwords, SSH keys and switch management IP.
! Follow 02_network_changes.md. This file does not save startup-config.
configure terminal
!
vlan 20
 name DNS_LAB
exit
!
interface GigabitEthernet1/0/1
 switchport trunk allowed vlan add 20
exit
!
interface GigabitEthernet1/0/27
 switchport trunk allowed vlan add 20
exit
!
! Preserve the validated source, both directions and original VLAN headers.
! Recreating this session briefly interrupts monitoring, not live forwarding.
no monitor session 1
monitor session 1 source interface GigabitEthernet1/0/27 both
monitor session 1 destination interface GigabitEthernet1/0/28 encapsulation replicate
!
! Existing native VLANs stay 999 toward SRX and 99 toward Proxmox.
! Apply this ACL only with verified analyst source IP 172.16.99.10.
ip access-list standard LAB_ANALYST_ONLY
 permit host 172.16.99.10
 deny any
exit
line vty 0 15
 access-class LAB_ANALYST_ONLY in
 transport input ssh
 login local
 exec-timeout 15 0
exit
!
clock timezone UTC 0 0
service timestamps log datetime msec show-timezone
logging buffered 131072 informational
logging source-interface Vlan99
end
!
! STOP: validate a NEW SSH login and VLAN/SPAN state before saving.
! Save separately with: copy running-config startup-config
